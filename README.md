# onewheel

Main

`setup()`

`loop()`

`log()`

IMU

`setupIMU()`

`updateIMU()`

`getRoll()`

`getPitch()`

`getYaw()`

`getHeading()`

Motor

`setupMotor()`

`updateMotor()`

`setTargetRPM(float)`

`getRPM()`

`getMotorBattVoltage()`

`getBattCurrent()`

`getMotorCurrent()`

`Battery`

`getBattVoltage()`

Leds

`setFrontLEDs(int, int, int)`

`setRearLEDs(int, int, int)`

Fsr

`sFrontFSRTriggered()`

`sRearFSRTriggered()`

Logging

??


